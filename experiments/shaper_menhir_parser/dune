(ocamllex
 (modules Lexer))

(menhir
 (modules Parser)
 (flags --strict -v -O 2 -la 2))

(executable
 (name shaper_menhir_parser)
 (modules shaper_menhir_parser parser lexer)
 (libraries shaper fmt))
