(jbuild_version 1)

(library
 ((name Fold)
  (public_name fold)
  (modules
   (Lex
    Local
    Compile
    Parser
    Interpreter
    Syntax
    OCaml))
  (libraries
   (sedlex
    pratt
    fmt
    threads
    lambda-term
    compiler-libs.bytecomp
    ppx_deriving.std
    options))
  (modes (byte native))
  (preprocess
    (pps
      (ppx_deriving.std
       sedlex.ppx
       ppx_inline_test
       ppx_expect)))
  (inline_tests)))

(executable
 ((name Main)
  (public_name fold)
  (modules (Main))
  (modes (byte native))
  (libraries
   (fold))))

;(executable
; ((name Shell)
;  (public_name fold-shell)
;  (modes (byte))
;  (modules (Shell))
;  (libraries
;   (fold
;    compiler-libs.toplevel
;    compiler-libs.bytecomp
;    compiler-libs
;    utop))))

